@AGENTS.md

# Claude Code Notes

- Shared repository instructions live in `AGENTS.md`; keep this file limited to
  Claude Code-specific workflow notes.
- For multi-file UI, API, auth, Prisma, or hidden-room changes, draft a short
  plan before editing and verify against the relevant tests afterward.
- Before committing source, config, workflow, script, package, or Prisma changes,
  run or account for `npm run check:docs`.
- For small documentation-only or narrowly scoped source edits, read the relevant
  files first and proceed directly.
- Avoid duplicating long project context here. Inspect `README.md`,
  `package.json`, and relevant files on demand instead.
